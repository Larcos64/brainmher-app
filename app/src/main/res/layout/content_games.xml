<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/parentTotal"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".application.ui.activities.Games"
    tools:showIn="@layout/activity_games">

    <LinearLayout
        android:id="@+id/progres_cont"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:visibility="visible">

        <ImageView
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/ic_logo_carta">

        </ImageView>

        <ProgressBar
            android:id="@+id/progressBar_init"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/contanedor_games"
        style="@style/parentTotal"
        android:visibility="gone"/>
</RelativeLayout>
